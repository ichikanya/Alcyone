/* Placeholder edition facts for local development.

   The packager regenerates this file per edition. It carries only public,
   non-secret identity used by the TV interface. The data directory, web port
   and autostart name belong to the service and are delivered separately in
   edition.json, so the frontend never learns filesystem paths. */
window.ALCYONE_EDITION = {
  appId: 'com.alcyone.vpn',
  serviceId: 'com.alcyone.vpn.service',
  core: 'xray',
  coreLabel: 'XRay',
  editionName: 'XRay Edition',
  title: 'Alcyone XRay',
  version: '4.0.3'
};
