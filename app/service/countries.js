'use strict';

/* Local source-tree bridge. The packager replaces this file with the exact
   shared browser module so the installed app and service use identical data. */
module.exports = require('../countries');
