name: Build and release editions

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "20"
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Syntax checks
        run: |
          node --check app/app.js
          node --check app/web/alcyone-web.js
          sh -n app/scripts/alcyonectl.sh
          sh -n CONTROL/preinst
          sh -n CONTROL/postinst
          sh -n CONTROL/prerm
          python -m py_compile build_ipk.py
      - name: JavaScript tests
        run: |
          for test_file in tests/*.test.js; do
            node "$test_file"
          done
      - name: Packaging tests
        run: python tests/build-editions.test.py

  build:
    needs: test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        edition:
          - xray
          - sing-box
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Build ${{ matrix.edition }} edition
        run: python build_ipk.py --edition "${{ matrix.edition }}" --output-dir dist
      - uses: actions/upload-artifact@v7
        with:
          name: alcyone-${{ matrix.edition }}
          path: dist/*.ipk
          if-no-files-found: error

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
      - name: Publish canonical release assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          version="${GITHUB_REF_NAME#v}"
          notes="docs/release-history/RELEASE_NOTES_${version}.md"
          # Matrix jobs verify both independent builds; release the exact bytes
          # referenced by the repository feed, manifests, and SHA256SUMS.
          assets=(
            "packages/Alcyone-XRay_${version}_all.ipk"
            "packages/Alcyone-sing-box_${version}_all.ipk"
          )
          for asset in "${assets[@]}"; do
            test -f "$asset"
          done
          if gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1; then
            gh release upload "$GITHUB_REF_NAME" "${assets[@]}" --clobber
          else
            gh release create "$GITHUB_REF_NAME" "${assets[@]}" \
              --title "Alcyone ${version}" \
              --notes-file "$notes"
          fi
